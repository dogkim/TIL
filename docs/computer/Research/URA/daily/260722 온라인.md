FTL(flash translation layer)
OS block 
-> inplace (HDD 시절)

기존에서 OS에서 관리하는 LBA(logical block address)가 있겠지
기존 값을 다른 곳으로 옮겨도 같은 LBA를 지님

![[LBA_to_physical_addr_mapping_v2.png]]

LBA와 피지컬 어드레스를 이어줘야겠죠?
그걸 맵핑

OS가 있고 device로 갈 수 있었는데, (이 구조는 동일해야함)
OS에서 어떤 디바이스를 쓰던간에 (HDD이건 SSD이건) 지원을 해줄 수 있음
OS에서 LBA로 줌
	근데 SSD입장에서는? 그게 어떤 physical address인데?
	FTL이 LBA에서 phy.addr 로 바꿔줌

저번에 말한 GC(garbage collecter)
LBA순서대로 저장되어있지는 않음

LBA를 물리위치로 저장하는 mapping table이 있음.
그래야 어떤 data가 요청이 들어와야 return하겠죠?
### 정리
HDD와 작동 방식이 다르기 때문에 LBA와 psy.addr를 이어주는 FTL이 필요하다
write될때 생성이 되고 read request될 때마다 접근, erase되면? 기존 entry는 지워진다

___
NAND에도 수명이 있음

TBW(Terabytes Written)
쓰기 수명

NAND 수명 있음 (program / erase cycle) -> PE cycle
	PE cycle이 써질 때 마다 열화가 됨

![[Floating_gate_transistor_diagram.png]]

지우기 과정에서의 고전압 과정에서
막아주는 역할을 하는 oxide가 wear out(닳음) 됨

![[Block_PE_cycle_diagram_v2.png]]

하나의 큰 Plane 안에 블럭이 여러개가 쭉 있을 텐데
블럭마다 업데이트 주기가 다름. GC가 자주 발생함

PE Cycle이 각 100번 일어났을 때
	cold는 그닥 오를일 없음
	hot한 block는 빠르게 block이 꽉 참. -> P/E cycle이 더 늘어남!

block마다 P/E cycle이 달라졌다고 가정.
어떤 한 block이 먼저 한도에 다다르면 에러가 엄청 생길수도 있음
-> P/E cycle을 균등하게 하자 -> (wear leveling 나중에 다룰 예정)

___
conv proc nvme io cmd

struct conv ftl
	ssd
	convparams
	ppa
	
	wp
	gc_wp
	왜 2개지

FTL에서 line 개념?
plane당 block