device 는 32G
물리적 용량은 32G 
OP(OverProvisioning)는 여유분을 메타데이터나 맵핑 테이블 등등에 사용
	-> 유저에게 공개 X
나머지 부분만을 fio 100% 줬을 때 사용할 수 있음

OP부분도 차야 GC가 발생하던데 왜 거기까지 가야 GC가 발생하는가?
	OP에는 오염되면 안되는 데이터가 있지 않냐
	-> 원래는 chip이16개 device size가 커지면 line크기가 커짐.
	일부 line만으로 충분히 커버된다는건가?

___

### DFTL 
- TLB 같은거 (caching 같은거)

mapping이 있음
	LBA <-> PPA 하는
다 저장하니까 너무 크다! (DRAM이나 SRAM에 올리기에)
필요한 mapping만 caching하자

짱 큰 mapping table이 있다고 하자.
SRAM size는 좀 작아.
다 못 넣으니까 SRAM에 자주 쓰이는 것만 올려.

![[DFTL_mapping_to_SRAM_sketch2.png]]

SRAM이 다 차면?
누구를 빼야하지? 잘 안쓰는 놈 나가.

caching 말하는것들
	locality를 말하죠?

그냥 쓰인놈 올리는 방식.
	얘는 쓰이네? 그럼 일단 넣어.
	TBL -> SRAM올리는거 문제 없음 

다 찼을때 어떻게 빼지? 

hit하면 
LRU
FIFO
Greedy caching 정책은 구현하기 나름이다!