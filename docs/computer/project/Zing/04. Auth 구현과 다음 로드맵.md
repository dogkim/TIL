### 지금까지 진행 상황

- **repo 마이그레이션**: 서희 소유(`helen112812/Excdiary`)에서 권한 문제로 `dogkim/Zing`(private)로 이전, 기존 커밋 히스토리 유지
- **컨벤션 정리**: `CLAUDE.md`에 네이밍 규칙(화면/컴포넌트 PascalCase, 유틸 camelCase), `async/await` 사용, 함수 리턴은 `{ data, error }` 형태로 통일, Prettier(`.prettierrc`) 적용
- **[기능명세서.md](기능명세서)**: `users`/`diaries` 테이블 스키마 + `signUp`/`signIn`/일기 CRUD API 계약 정의 (병렬 작업 시 프론트-백엔드 안 부딪히게 하는 기준 문서)
- **Supabase 테이블 생성 완료**: `users`, `diaries` (컬럼까지 명세서 그대로 반영)
- **`lib/types.ts` 완료**: `User`, `Diary`, `SignUpInput`, `CreateDiaryInput`, `UpdateDiaryInput`
- **TypeScript 기초 학습 노트**: [Web-Frontend/05. 타입스크립트 기초](../../Web-Development/Web-Frontend/05.%20타입스크립트%20기초)에 정리 (비동기, 구조분해할당, `type`/`Omit`/`Pick`, snake_case↔camelCase 등)
- **`lib/auth.ts` 작업 중**: `signUp` 함수 초안 작성, 아직 저장/테스트 전

### `auth.ts` 이후 큰 그림 (기존 로드맵 기준 현재 위치)

기존 로드맵([02. 역할 분담과 로드맵](02.%20역할%20분담과%20로드맵)):
```
1. DB 스키마 설계          ✅ 완료
2. 인증(Auth)              🔨 진행 중 (지금 여기)
3. 일기 CRUD               ⬜ 다음
4. 피드 UI                 ⬜ (서희 UI/UX 완료 후 연동)
5. 징(매칭) 로직            ⬜
6. 번역 PoC                ⬜
7. 단계적 공개/소통 해금     ⬜
8. 채팅, 알림               ⬜
```

### 다음 단계 (순서대로)

1. **`lib/auth.ts` 완성**
   - `signUp` 코드 저장 및 실제 회원가입 테스트 (Supabase Auth + `users` 테이블 insert 둘 다 성공하는지)
   - `signIn` 함수 추가 구현
2. **`lib/diaries.ts` 구현** — 일기 CRUD 4종
   - `createDiary`, `getDiaries`, `updateDiary`, `deleteDiary`
   - 패턴은 `signUp`과 동일: `async/await` + `{ data, error }` + snake_case 매핑
3. **RLS 정책 설정** — 지금 테이블은 RLS는 켜져 있지만 정책(Policy)이 없어서 아무도 접근 못 하는 상태. 2~3번 함수 테스트하려면 최소한의 정책 필요 (예: 로그인한 본인만 자기 `users`/`diaries` row 읽기·쓰기 가능하도록)
4. **화면(`screens/`) 코드와 연결** — 서희 님 Figma 작업 진행 상황 보고, 화면 완성되는 대로 `signUp`/`createDiary` 등을 버튼에 연결(기능 구현 단계)
5. 이후 로드맵 5~8번(매칭, 번역, 공개 단계, 채팅)은 위 기반이 안정된 다음 순서대로

### 참고 — 아직 미정으로 남아있는 정책 항목
[기능명세서.md](기능명세서)의 "정책/제약 규칙" 섹션 그대로 — 동시 매칭 인원 상한, 소통 해금 단계, 채팅 범위, 글감 도입 여부, 단계적 공개 기준. 로드맵 5번(매칭) 들어가기 전까지는 결론 필요.
