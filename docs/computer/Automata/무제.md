$G = (V,S,T,P)$
w G로부터 inf가능하다면 A root로 하고,
w를 yield parse tree 존재

inference length 를 가지고 inductive proof

1. inf len =1의 경우
    $A \to w$ 가 원래 존재해야함
    $A$가 $w$각각의 요소들을 child로 가져야함  

    parse tree 정의
    그 그림에 있으면
    A의 child $a_1, a_2, a_3$ 가 반드시 있어야함
2. inf len >= 2
    n번까지 수행한 inference에 대해 parse tree가 존재 : IH
n+1번째 
$A \to x_1, x_2, ~x_k$ 
w = w_1, w_2 ... w_k
(각 w_i 가 x_i로부터 infer 할 수 있음) 

-> $A \to x_1, x_2, ~x_k$ 는
각 w_i는 n번 이내에 inferred된 string
x_i를 root하고 w_i를 yield하는 parse tree 존재

A-> x_1, x_2, ... x_k
___

Th) A를 root, w를 yield tree가 있으면 (G = (V, T, S, P))
왜 root를 S로 안두고 A로 두나요?
    S -> AB
    A -> aA
    경우 A -> aA의 경우도 포함 하기 위해.. (일반성을 위해) 

I G(CFG) 에 대하 A * => lm w 가 가능(존재)하다
pr) tree 의 height를 가지고 induction
1. h = 1
    A 바로 밑에 leaf만을 child로 가지는 child는 w의 구성요소
    이건 p : A -> w
    A => w
2. h >= 2
    IH: height h 까지의 parse tree(root X_i) 가 w_i를 yield한다면 h+1?
    
    A-> x_1, x_2, ... x_k
    A 밑에 x_1, x_2 ... x_k를 child (높이 1 차지)
    그 밑에 높이가 h인 subtree가 만들어지겠죠..?
    각 subtree는 각각 w_1, w_2 ... w_k을 나타내도록 쪼개짐
    w 가 w_1, w_2... w_k로 break up된 것
    A =>(lim) x_1x_2... x_k \*=>lim w_1x_2... x_k => lim w_1w_2...w_k (= w)

___
어떤 i에 대해서 A \*=>lim w_1w_2...w_i-1X_i+1X+i+2
induction i = 0 
    A \*=>lim X_1 X_2 ... X_k
i >= 1
    A\*=>lim w_1w_2...w_i-1 X_i X_i+1 ... X_k 
X_i \contain T
X_i ]contain V ( X_i \*=>lim w_i : X_i =>lim a_1 )
?

___
context free 면 앞, 뒤에 뭐가 오는지 알아야..함
context free는 variable이 하나 밖에 없으니까..
그게 어떤식으로 변해가는지만 알면 된다..
=> 문맥과 상관이 없음 -> parse tree도 가능

w를 

1개의 parse tree에서 여러 derivation이 나올 수 있음..
가령 leftmost, rightmost의 경우

1개의 derivation에서는 1개의 parsetree가 나온다..
___
A \*=>G w 이면 w가 A의 language라는 것은 inference할 수 있다
증명) induction (derivation의 길이)
    1) 길이가 1인 경우
        A => w 
        production안에 A-> w가 있어서 
        w가 A의 language라는 것을 inferance할 수 있다.
    2) 길이가 2이상인 경우
        (IH) 길이 n까지의 derivation에 대해서는 inference가 가능하다
        즉, A=> x_1, x_2 ... x_k \*=> w / (w_1 w_2 ... w_k) X_i \*=>w_i 
        x_1 x_2 x_k \*=> w_1w_2...w_k
        n >= derivation
        x가 A의 language
        모르겠는데요?

A => x_1 x_2 x_k \*=> w 
w는 w_1 w_2 w_k (X_i \*=> w_i)로 breakup 가능하다

inductive적으로 증명 된다
dervation number를 기준으로 induction하면 된다..
(해보던가)

___
## Ambiguity
여러 derivation을 가지는 경우

parse tree를 각각의 경우에 대해서 만들 수 있다..
그러나 parse tree가 다른 의미를 가질 수 있다

CFG가 Ambiguous 함
    어떤 w를 만들수있는 parse tree가 여러개인 경우
    
CFL L inherently ambiguous함..
    그것의 Grammar가 모두 Ambiguous한 경우 

직접 제거 할 수 있지만, 그것을 일반화된 공식을 통해 제거할 수는 없음
어떤 CFL은 항상 ambiguous할 수도 있다

___
left most나 right most라고 해서 ambiguous하지 않은 것은 아니다..

Term 
T \* F, F\* F 로 나타나야함,,
T\* T면,, T \* T \* T 면 ambiguous 해짐..
명확히 모르겠음..
비상